{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start patient-service-db",
      "type": "shell",
      "command": "docker run --name patient-service-db -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -p 5000:5432 -d postgres:latest",
      "problemMatcher": [],
      "detail": "Start PostgreSQL container for patient-service"
    }
  ]
}
